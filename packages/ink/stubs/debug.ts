/**
 * No-op stub replacing Claude Code's internal debug logger.
 * Replace with your own logger if needed.
 */
export function logForDebugging(_message: string, _opts?: { level?: string }): void {
  // no-op
}
